# MIDI Composer プロジェクト規約 (GEMINI.md)

本ファイルは、Antigravityエージェントが本プロジェクト（MIDI Composer）で開発・保守作業を行う際に**常時適用されるマスターガイドライン（ハーネス）**です。

---

## 1. ルールブック自動参照プロトコル (Rulebook Reference Protocol)

本プロジェクトには、機能やコンポーネントごとの詳細な設計基準を定めた**ルールブック**が設置されています。
エージェントは以下のルールブック参照プロトコルを厳格に遵守してください。

> [!IMPORTANT]
> **ルールブック参照の義務化**
> 1. エージェントはタスクに着手する際、プロジェクト内の `.agents/rules/` または `rules/` ディレクトリを確認し、関連するルールブックが存在する場合は**必ずそのルールブックを読み込み、記載された設計基準・制約に準拠して作業を行うこと**。
> 2. ルールブックに定義された制約事項や禁止事項は、一般的な実装パターンよりも優先して適用される。

---

## 2. モーダル / ダイアログ UI 作成・改修時のハーネス

新たにモーダルを作成、または既存モーダルを改修する際は、必ず以下のルールブックを参照してください：
👉 [モーダルUI設計規約ルールブック](.agents/rules/ui-modal-design.md)（または `rules/ui-modal-design.md`）

### 最重要遵守事項（要約）
* **背景色の透過処理は厳禁**:
  * モーダル本体（カード）は、背後が透けて見えなくなるように必ず**完全不透明な白地背景**（`#ffffff` / `bg-white`）とし、`style={{ backgroundColor: '#ffffff', opacity: 1 }}` を明示すること。
  * `opacity < 1` や `bg-slate-900/80`、`bg-white/90` などの半透明・透過クラスをモーダルカード本体に指定してはならない。
* **背景オーバーレイ（バックドロップ）**:
  * `style={{ backgroundColor: 'rgba(0, 0, 0, 0.6)' }}` を使用し、`backdrop-blur`（ぼかし）は使用しない。
* **高コントラストな文字色**:
  * 白地背景のため、見出しは `text-slate-900`、本文は `text-slate-800` など十分なコントラストを持つ濃色テキストを指定すること。
* **完了前セルフチェック**:
  * モーダル実装後は、ルールブック記載のチェックリストを全項目クリアしていることを確認すること。

---

## 3. プロジェクト概要・技術スタック

* **フレームワーク**: React 18 + TypeScript + Vite
* **スタイリング**: Tailwind CSS
* **オーディオ**: Web Audio API, SoundFont (spessasynth_lib / soundfont2), MP3 Exporter (lamejs)
* **楽譜描画**: VexFlow 4
* **アイコン**: Lucide React

---

## 4. 品質・動作確認ルール

1. コード変更後は、構文エラーや型エラーが発生していないかビルドテスト（`npm run build`）を実行して確認すること。
2. 既存の機能（MMLエディタ、楽譜表示、MIDI/MP3出力、再生コントロールなど）を破壊しないように注意すること。
